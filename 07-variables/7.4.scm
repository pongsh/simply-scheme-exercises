; This function adds the squares of two numbers.
; It swaps the + operator for the * operator and vice versa so
; It is actually doing this (+ (* a a) (* b b))

(define (sum-square a b)
  (let ((+ *)
        (* +))
    (* (+ a a) (+ b b))))